/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "images.unsplash.com",
      "picsum.photos",
      "cdn.example.com",
      "test.com",
      "localhost",
    ],
  },
};

export default nextConfig;
